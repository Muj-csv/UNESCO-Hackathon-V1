/* ============================================================================
   OWNER: T4 (verify before you share).

   Ledger.tsx already composes this on a single line, so T2 can rewrite the
   death rows freely and T4 never opens that file.

   T4: read round.verifyChoice and the ledger result, then state what happened
   — the atom they would have checked, and whether this claim had already lost
   it. Four cases: chose the first loss, chose a later loss, chose a survivor,
   nothing was lost.

   Never score this. No right/wrong marking, no points, no "correct!". State
   what happened and let the player draw the conclusion.
   ========================================================================== */

export default function VerifyFeedback() {
  return null;
}
