/* ============================================================================
   OWNER: T9 (prediction stake).

   Prediction.tsx already composes this, so T9 fills in its own file.

   T9: five atom buttons with their one-line descriptions. Every player picks,
   privately, which atom they think dies first. Five seconds, one tap.

   Store per player, per round in round.predictions. Report the room's
   aggregate at the debrief — "4 of 8 predicted HEDGE" — and NEVER rank an
   individual. Same rule as everywhere else in the app.
   ========================================================================== */

export default function PredictionPrompt() {
  return null;
}
