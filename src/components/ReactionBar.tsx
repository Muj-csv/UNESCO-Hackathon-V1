/* ============================================================================
   OWNER: T9 (live reactions).

   Reveal.tsx already composes this, so T9 fills in its own file.

   T9: three or four reactions, no more. Suggested: 😬 · 👀 · 😂 · 🎯
   In room mode they broadcast through the poll; in pass-and-play the shared
   screen collects taps from whoever is holding it.

   Reactions are EPHEMERAL. Never stored, never counted, no "most reacted hop"
   superlative. The moment it becomes a metric it becomes a score, and this
   application has no score anywhere.
   ========================================================================== */

export default function ReactionBar() {
  return null;
}
